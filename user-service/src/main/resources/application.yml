server:
  port: 60100
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/gk_user?useSSL=true&serverTimezone=UTC
    username: temp
    password: 123
  data:
    redis:
      host: localhost
      port: 6379
mybatis-plus:
  mapper-locations:
    - classpath:mapper/**.xml